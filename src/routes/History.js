import { Card } from "components/Card";
export const History = () => {
    return (
        <div className="video-card-container">
            <Card type="history" />
            <Card type="history" />
            <Card type="history" />
            <Card type="history" />
            <Card type="history" />
        </div>
    );
};
